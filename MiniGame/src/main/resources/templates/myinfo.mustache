{{!
    @FileName : myinfo.mustache
    @Project  : MiniGame
    @Author   : 고다은
}}

{{> layouts/header}}

<div style="text-align: center; padding: 20px;">
    <h2>내 정보 관리</h2>

    <form action="/myinfo/update" method="post" style="width: 300px; margin: 0 auto; text-align: left;">

        <div style="margin-bottom: 10px;">
            <label style="display:block; font-weight:bold;">아이디</label>
            <input type="text" name="id" value="{{member.id}}" readonly
                   style="width: 100%; background-color: #e9ecef; color: #555; cursor: not-allowed;">
            <small style="color: gray;">* 아이디는 변경할 수 없습니다.</small>
        </div>

        <div style="margin-bottom: 10px;">
            <label style="display:block; font-weight:bold;">비밀번호</label>
            <input type="text" name="password" value="{{member.password}}" style="width: 100%;">
        </div>

        <div style="margin-bottom: 20px;">
            <label style="display:block; font-weight:bold;">이메일</label>
            <input type="email" name="email" value="{{member.email}}" style="width: 100%;">
        </div>

        <button type="submit" style="width: 100%; padding: 10px; background-color: #007bff; color: white; border: none; cursor: pointer;">
            정보 수정 완료
        </button>
    </form>

    <hr style="margin: 30px 0;">

    <div>
        <p style="color: gray; font-size: 12px;">더 이상 게임을 이용하지 않으시려면...</p>

        <a href="/myinfo/delete" onclick="return confirm('정말로 탈퇴하시겠습니까? 모든 데이터가 삭제됩니다.');">
            <button style="background-color: #dc3545; color: white; border: none; padding: 10px 20px; cursor: pointer;">
                🚨 회원 탈퇴 (계정 삭제)
            </button>
        </a>
    </div>

</div>

{{> layouts/footer}}